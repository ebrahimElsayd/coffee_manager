# Legacy migrations

These files belong to the first manager-only schema and are retained for
historical reference. They are not part of the active Supabase migration
stream and must not be pushed again.

The canonical migrations live in `D:\tester\supabase\migrations` and use the
shared customer/manager schema (`menu_products`, `table_sessions`, `orders`,
`order_items`, and `payments`).
